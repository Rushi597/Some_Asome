#include "server.h"

/**
 * @brief Main function to start the chat server.
 *
 * This function calls the startServer function to initialize and run the server.
 *
 * @return int Exit status.
 */
int main() {
    startServer();
    return 0;
}
